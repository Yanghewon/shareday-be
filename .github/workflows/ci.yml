name: CI

on:
  pull_request:
    branches: [ dev, main ]
    types: [opened, reopened, ready_for_review, synchronize]
  push:
    branches: [ dev ]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    name: build-and-test
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'
          cache: gradle

      # ✅ 먼저 실행 권한 부여
      - name: Grant execute permission for gradlew
        run: chmod +x ./gradlew

      # ✅ wrapper 검증
      - name: Validate Gradle wrapper
        run: ./gradlew wrapper --gradle-version 8.7 --validate

      - name: Build & Test
        run: ./gradlew clean build --stacktrace

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-reports
          path: |
            **/build/reports/tests/**
            **/build/test-results/**
            **/build/reports/jacoco/**
